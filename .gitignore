# dependencies
/node_modules

# next.js
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# secrets & environment
.env
.env*.local

# local, per-user data — profiles, subscribers, queue, generated content, logs.
# Never committed; the app recreates data/ and falls back to seed content.
/data/
/src/content/generated-edition.json
/src/content/personalized-edition.json

# os / editor
.DS_Store
*.pem
